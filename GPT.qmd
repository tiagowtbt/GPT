---
title: "GPT"
---

```{r}
library("readtext") #pacote para ler o texto
```

Primeiro temos que importar nossos dados

https://colab.research.google.com/drive/1JMLa53HDuA-i7ZBmqV7ZnA3c_fvtXnx-?usp=sharing#scrollTo=O6medjfRsLD9

```{r}
text <- readtext("input.txt") 

text$text <- enc2utf8(text$text)
nchar(text$text) # numero de caracteres no texto

#cat(text$text, "\n", sep = "")

chars <- sort(unique(strsplit(text$text, "")[[1]])) # nosso vocabulario

cat(chars)
#cat(substr(text$text, 1 ,1000), "\n", sep = "") # pri

```

```{r}
# criando os codificadores e decodificadores

stoi <- order(chars)

my_function <- encode() { 
  
  
}

```

